# 超越大陆项目最新全量梳理文档

> 文档版本：2026-05-04
> 
> 代码基线：`E:/project/dldl-new`
> 
> 文档目标：基于**当前仓库真实代码**，完整梳理项目结构、核心依赖、入口链路、面板系统、状态流、战斗流、资源流、存档键位与当前真实缺口。
> 
> 说明：本文档优先以代码现状为准；若旧文档、旧测试说明与当前实现不一致，以本文为准。

---

## 1. 项目一句话说明

`dldl-new` 是一个基于 `Vue 3 + Pinia + Phaser 3 + TypeScript + Vite` 的**斗罗题材单机网页 RPG 原型**：

- `Vue` 负责 HUD、面板、创建角色、背包、任务、设置等复杂 UI；
- `Phaser` 负责主场景显示与实时战斗；
- `Pinia` 作为 Vue 与 Phaser 共享状态中台；
- `localStorage` 负责全部本地存档与部分模块级持久化；
- 整体是**无后端、无路由、无账号体系**的本地单机结构。

---

## 2. 当前最重要的架构结论

### 2.1 项目不是路由驱动，而是“单页壳 + 动态面板 + Phaser 场景”

当前代码中**没有 `vue-router`**。页面切换主要依赖两层机制：

1. `uiStore.currentPanel` 决定当前显示哪个 Vue 面板；
2. `Phaser.Scene` 决定当前处于主城场景还是战斗场景。

### 2.2 Vue 和 Phaser 共享一套 Pinia 状态

- `stores/game.ts` 保存角色、背包、魂环、魂骨、快捷栏、地图 ID、待选魂技等核心游戏状态；
- `stores/ui.ts` 保存当前面板、日志频道、角色创建状态等 UI 状态；
- `BattleScene.ts` 会直接读写 `gameStore` 和 `uiStore`，因此战斗不是独立系统，而是主状态的一部分。

### 2.3 项目是本地单机，状态持久化分散在多个 `localStorage` key

主存档不只一个键，除了 `chaoyue-dalu-save` 外，任务、宗门、竞技、设置等模块还各自写自己的键。也就是说：

- **角色主数据**集中在 `gameStore.saveGame()`；
- **模块附属数据**分散在各面板文件内直接读写 `localStorage`。

### 2.4 当前文档与当前代码之间存在实际偏差

当前仓库中已有 `项目完整文档.md` 和 `测试流程.md`，但它们已经**部分落后于现在的代码**。例如：

- `CodexPanel.vue` 真实存在，但目前没有菜单入口；
- `SettingsPanel.vue` 展示了 `C/B/W/K/Q` 热键说明，但代码里并没有接入完整的全局快捷键切面板逻辑；
- `turnBattleRules.ts` 仍在仓库中，但当前实际战斗使用的是实时规则文件 `realtimeBattleRules.ts`。

---

## 3. 技术栈与运行脚本

### 3.1 依赖栈

来自 `package.json`：

- 运行时依赖
  - `vue`
  - `pinia`
  - `phaser`
- 开发依赖
  - `vite`
  - `@vitejs/plugin-vue`
  - `typescript`
  - `vue-tsc`
  - `tailwindcss`
  - `@tailwindcss/vite`

### 3.2 脚本说明

- `npm run dev`
  - 启动 Vite 开发服务器。
- `npm run build`
  - 先跑 `vue-tsc -b`，再跑 `vite build`。
- `npm run preview`
  - 预览构建产物。
- `npm run assets:manifest`
  - 从 `gameData.ts` 导出资产清单。
- `npm run assets:prompts`
  - 导出资产清单 + 图像提示词。
- `npm run assets:atlases`
  - 导出清单 + 提示词 + 图集分组。
- `npm run assets:verify`
  - 基于 `asset-manifest.json` 验证资源文件是否缺失。

### 3.3 构建/开发配置

- `vite.config.ts`
  - 注册 `vue()` 和 `tailwindcss()` 插件；
  - 配置别名 `@ -> src`；
  - 开发端口为 `3000`；
  - `open: true`。
- `tsconfig.json`
  - `strict: true`；
  - `moduleResolution: bundler`；
  - `noEmit: true`；
  - `@/* -> src/*`。
- `env.d.ts`
  - 提供 `vite/client` 与 `*.vue` 类型声明。

---

## 4. 目录结构总览

### 4.1 根目录

```text
E:/project/dldl-new
├─ src/                          主源码
├─ docs/                         Superpowers 计划与设计稿
├─ scripts/                      资源清单/提示词/图集/校验脚本
├─ public/                       公共资源（当前很轻）
├─ dist/                         已构建产物
├─ tmp/                          临时资源/审计中间产物
├─ index.html                    HTML 入口
├─ package.json                  依赖与脚本
├─ vite.config.ts                Vite 配置
├─ tsconfig.json                 TypeScript 配置
├─ 项目完整文档.md               旧版总文档
├─ 测试流程.md                   旧版测试文档
└─ 项目最新全量梳理文档.md       当前这份新文档
```

### 4.2 `src` 主结构

```text
src
├─ assets/
│  ├─ art-direction/             资源索引、生成路径、资产文档
│  ├─ avatars/                   角色头像
│  ├─ backgrounds/               场景背景图
│  ├─ battle/                    战斗切图/战斗资源
│  ├─ generated/                 AI 生成资源（人物、技能、物品、宗门等）
│  ├─ icons/                     武魂/技能/魂骨/系统图标
│  ├─ maps/                      世界地图背景
│  └─ ui_generated/              生成按钮资源
├─ data/
│  ├─ gameData.ts                核心静态数据源
│  └─ displayData.ts             显示文案与标签映射
├─ game/
│  ├─ audio/                     音频管理
│  ├─ scenes/                    Phaser 场景
│  ├─ utils/                     战斗/掉落/魂骨/图形辅助逻辑
│  └─ config.ts                  Phaser 配置工厂
├─ stores/
│  ├─ game.ts                    游戏主状态
│  └─ ui.ts                      UI 状态与面板注册
├─ types/
│  └─ game.ts                    类型定义
├─ ui/
│  ├─ components/                通用组件
│  ├─ hud/                       顶/左/底 HUD
│  ├─ panels/                    各业务面板
│  ├─ CharacterCreate.vue        角色创建流程
│  └─ GameContainer.vue          Phaser 容器与创建角色切换器
├─ App.vue                       页面总壳
├─ main.ts                       Vue 入口
└─ style.css                     全局样式与主题变量
```

---

## 5. 核心入口链与文件引入关系

这一节专门回答“**文件从哪里引入、由谁触发、往哪里流**”。

### 5.1 浏览器入口链

1. `index.html`
   - 提供 `#app` 挂载点；
   - 通过 `<script type="module" src="/src/main.ts">` 进入应用。
2. `src/main.ts`
   - 引入 `App.vue`；
   - 引入 `style.css`；
   - `createApp(App)` 后 `app.use(createPinia())`；
   - 绑定 `pointerdown` / `keydown` 用于首次解锁音频上下文。
3. `src/App.vue`
   - 作为全局页面壳，导入：
     - `GameContainer.vue`
     - `TopBar.vue`
     - `LeftMenu.vue`
     - `BottomBar.vue`
     - `CityPanel.vue`
     - `useUIStore()`
   - 根据 `uiStore.currentPanel` 决定显示主城覆盖层或业务面板浮层。
4. `src/ui/GameContainer.vue`
   - 导入：
     - `createGameConfig`（Phaser 配置工厂）
     - `useGameStore` / `useUIStore`
     - `CharacterCreate.vue`
     - `RingSkillSelectPanel.vue`
   - 负责：
     - 启动时尝试 `gameStore.loadGame()`；
     - 如果已有角色则直接进入游戏；
     - 如果没有角色则显示创建角色流程；
     - 创建 Phaser 实例并挂到 `window.__phaserGame`。

### 5.2 Phaser 启动链

- `GameContainer.vue` -> `createGameConfig()` -> `new Phaser.Game(...)`
- `createGameConfig()` 导入并注册：
  - `BootScene`
  - `GameScene`
  - `BattleScene`
- `BootScene.create()` 最终 `scene.start('GameScene')`
- 战斗切换由 `battleSceneFlow.ts` 负责：
  - `startBattleScene()`：停 `GameScene`，启 `BattleScene`
  - `returnFromBattleScene()`：停 `BattleScene`，启 `GameScene`
  - `stopBattleScene()`：强制结束战斗并回主场景

### 5.3 面板加载链

- `LeftMenu.vue` / `TopBar.vue` / `CityPanel.vue` / 各业务面板按钮
  -> 调用 `uiStore.openPanel(panelName)`
  -> `uiStore.loadPanelComponent(panelName)`
  -> 动态 `import('@/ui/panels/xxx.vue')`
  -> `App.vue` 用 `<component :is="uiStore.currentPanelComponent" />` 渲染。

也就是说，**所有业务面板的统一注册中心是 `stores/ui.ts`**。

---

## 6. 核心文件依赖表

| 文件 | 主要导入 | 主要职责 | 谁使用它 |
| --- | --- | --- | --- |
| `main.ts` | `App.vue`、`style.css`、`audioManager` | 创建 Vue 应用并解锁音频 | `index.html` |
| `App.vue` | `GameContainer`、`TopBar`、`LeftMenu`、`BottomBar`、`CityPanel`、`uiStore` | 页面总布局与浮层容器 | `main.ts` |
| `GameContainer.vue` | `createGameConfig`、`gameStore`、`uiStore`、`CharacterCreate`、`RingSkillSelectPanel` | 角色创建与 Phaser 生命周期 | `App.vue` |
| `game/config.ts` | `BootScene`、`GameScene`、`BattleScene` | Phaser 配置工厂 | `GameContainer.vue` |
| `stores/game.ts` | `types/game`、`gameData`、`spiritRingDrops`、`soulBoneCrafting` | 游戏主状态与主存档 | 几乎全部业务模块 |
| `stores/ui.ts` | `audioManager`、动态 `import()` 各面板 | 当前面板、日志、动态注册 | `App.vue`、各 HUD/面板 |
| `data/gameData.ts` | `types/game`、`icon-paths` | 武魂/魂技/魂骨/地图/物品静态数据 | `stores`、面板、战斗 |
| `data/displayData.ts` | `types/game` | 展示层中文标签与描述 | 多个面板/HUD |
| `BattleScene.ts` | `gameStore`、`uiStore`、`ITEMS`、`SKILLS`、`audioManager`、`battleVisuals`、多个规则工具 | 实时战斗核心 | `battleSceneFlow.ts` 间接触发 |
| `background-paths.ts` | 背景图片资源 | 导出背景 URL | `CharacterCreate`、`CityPanel`、`BootScene`、`WorldMapPanel` |
| `icon-paths.ts` | `import.meta.glob` 扫描图标 | 提供图标解析与占位图 | 几乎全部 UI |
| `generated-paths.ts` | `import.meta.glob` 扫描 AI 生成资源 | 提供生成按钮/NPC/人物/宗门图片路径 | 多个业务面板与战斗 UI |
| `GameIcon.vue` | 无业务依赖 | 图标展示、失败降级、品质发光 | 全项目图标统一组件 |

---

## 7. 场景层：Phaser 的真实职责

### 7.1 `BootScene.ts`

职责：资源预加载与程序化纹理初始化。

核心行为：

- 显示简易加载条与进度文本；
- 预加载：
  - 主城背景 `bg_city_wuhun`
  - 战斗背景 `bg_battle_xingdou_outer`
  - 战斗角色/怪物贴图与特效资源
- `create()` 时调用 `generateAllTextures(this)`：
  - 生成等距地块纹理
  - 生成程序化角色纹理
  - 生成程序化怪物纹理
  - 生成程序化 NPC 纹理
- 最后切到 `GameScene`。

### 7.2 `GameScene.ts`

职责：主场景背景与环境氛围，不承载复杂主城逻辑。

当前行为：

- 绘制主城背景图；
- 加一层暗色覆盖；
- 创建几条“环境消息”文本漂浮效果，增强活跃感；
- 预留 `setAutoFight()` / `isAutoFighting()` 两个接口，但目前**没有外部调用者**。

这说明：**当前主城交互主要发生在 Vue 浮层里，不在 Phaser 主城场景里。**

### 7.3 `BattleScene.ts`

职责：项目最重的核心逻辑文件，负责**实时战斗**。

#### 7.3.1 入口与初始化

`init(data)` 接收两种战斗上下文：

- 狩猎战：`monsterId + region`
- 竞技战：`arenaOpponent`

初始化时会：

- 构造 `this.monster`；
- 判断 `battleMode` 是 `hunt` 还是 `arena`；
- 清空技能冷却、怪物技能冷却、状态效果；
- 重置时间轴：`battleClock`、锁定时间、自动战斗状态；
- 构建怪物技能表；
- 记录胜负回调 `onVictory` / `onDefeat`。

#### 7.3.2 `create()` 做了什么

- 清空旧 children / tween / time events；
- 切战斗 BGM；
- 向 `uiStore` 写入战斗日志“遭遇某怪物，战斗开始”；
- 创建：
  - 背景与底部战斗信息区
  - 战斗标题
  - 怪物头部信息与血条
  - 玩家头部信息与血/蓝条
  - 双方战斗单位
  - 技能栏与攻击按钮
  - 数字键绑定
- 注册场景关闭清理逻辑。

#### 7.3.3 `update()` 驱动循环

每帧会：

- 累加 `battleClock`；
- 移除过期的 `effects`；
- 监听数字键释放魂技；
- 如果 `autoBattle = true`，自动尝试放魂技和普通攻击；
- 驱动怪物行动；
- 刷新技能栏冷却状态；
- 刷新底部状态提示。

#### 7.3.4 玩家攻击流

普通攻击：

- `performPlayerAttack()` 检查：
  - 战斗是否结束
  - 玩家动作冷却是否结束
  - 锁定时间是否结束
- 然后：
  - 计算下一次普攻时间；
  - 延迟调用 `doNormalAttack()`；
  - `doNormalAttack()` 内部用 `calculatePlayerDamage()` 算伤害；
  - 扣怪物血；
  - 播放伤害数字、命中特效、日志；
  - 检查怪物死亡。

魂技释放：

- `castPlayerSkill(slot)`：
  - 从角色 `rings[slot - 1]` 找到当前槽位魂环；
  - 通过 `ring.skillId -> SKILLS` 找到魂技定义；
  - 检查冷却和 MP；
  - 扣 MP；
  - 设置技能冷却；
  - 延迟调用 `resolvePlayerSkill(skill)`；
- `resolvePlayerSkill(skill)` 会按 `skill.type` 分支处理：
  - `damage`：直接伤害
  - `control`：控制怪物并可附带伤害
  - `debuff`：给怪物挂减益效果
  - `buff`：给玩家挂增益效果
  - `heal`：治疗玩家
- 最后保存主存档并检查怪物死亡。

#### 7.3.5 怪物攻击流

- `tryMonsterAction()` 基于怪物锁定时间、下次行动时间、控制时间判断是否可出手；
- 决定是普通攻击还是释放怪物技能；
- 怪物普通攻击会先走玩家闪避判定；
- 怪物技能伤害基于 `monster.atk * damageMultiplier` 计算。

#### 7.3.6 胜利、失败、逃跑

怪物死亡 `monsterDeath()`：

- 给玩家加经验与金币；
- 按掉落表滚普通掉落；
- 如果当前是狩猎战且玩家有空魂环位，则额外掉落**对应等级魂环道具**；
- 播放胜利音效；
- 写战斗日志；
- 打开结果弹层；
- 点击“继续冒险”后执行 `onVictory` 回调。

玩家失败 `playerDefeat()`：

- 角色血量恢复到 `maxHp * 0.35`，最低 1；
- 保存主存档；
- 打开失败弹层；
- 点击“继续冒险”后执行 `onDefeat`。

主动逃跑 `escapeBattle()`：

- 不给奖励；
- 结果弹层标题为“已脱离战斗”；
- 回调优先 `onDefeat`，否则回退到 `onVictory`。

#### 7.3.7 战斗日志流

`BattleScene.ts` 并没有自己维护独立日志容器，而是统一调用：

- `useUIStore().pushLog('battle', text)`

所以底部聊天栏的“战斗”标签，本质上是 `BattleScene` 往 `uiStore.logs` 里推文本。

---

## 8. 状态层：`gameStore` 与 `uiStore`

### 8.1 `stores/game.ts`：游戏主状态中台

#### 8.1.1 状态字段

- `character`
- `inventory`
- `quickSlots`
- `currentMapId`
- `pendingRingChoice`

#### 8.1.2 计算字段

- `hasCharacter`
- `rankTitle`
- `spiritDef`

#### 8.1.3 核心动作

##### 角色与属性

- `createCharacter(name, faction, spiritId)`
  - 根据武魂基础属性生成角色；
  - 初始化药品与快捷栏；
  - 调用 `recalculateStats()`；
  - 存档。
- `recalculateStats()`
  - 以武魂基础属性 + 等级成长 + 魂骨加成为基础；
  - 重新计算：
    - 四维
    - HP/MP 上限
    - 攻防
    - 暴击/闪避/攻速
    - 综合战力
  - `combatPower` 还会把魂环年份折算进来。

##### 背包与快捷栏

- `addItem()` / `removeItem()`
- `addSpiritRingItem()`：为带年份的魂环道具单独入包
- `canAssignToQuickSlot()`：当前仅允许 `potion`
- `assignQuickSlot()` / `clearQuickSlot()`
- `useQuickSlot()` -> 委托到 `usePotionItem()`
- `usePotionItem()` 支持：
  - `hp_potion_*`
  - `mp_potion_*`
  - `exp_potion_*`
  - `allheal_potion`

##### 魂环与魂技选择

- `nextAbsorbableRingSlot()`：计算当前可吸收的下一个魂环位
- `openRingChoiceFromItem(itemId, year?)`
  - 从魂环道具生成 `pendingRingChoice`
  - 依据主武魂系别筛出该槽位可选魂技
- `confirmRingChoice(skillId)`
  - 生成 `SpiritRing`
  - 添加到角色身上
  - 消耗背包中对应魂环道具
  - 重算属性并存档
- `cancelRingChoice()`
  - 关闭待选状态

##### 魂骨

- `equipBone()` / `unequipBone()`
- `craftSoulBone(recipeId)`
  - 基于 `soulBoneCrafting.ts` 计算是否可合成；
  - 自动找空魂骨位；
  - 自动抽一个合适魂骨并直接装备。

##### 经验与金币

- `addExp(amount)`
  - 支持连续升级；
  - 升级后会满血满蓝；
  - 每级后重新计算属性。
- `addGold(amount)`

##### 存档

- `saveGame()`
  - 主存档键：`chaoyue-dalu-save`
- `loadGame()`
  - 读取后会做：
    - 背包年份字段标准化
    - 快捷栏标准化
    - 魂环颜色与槽位标准化
    - 属性重算
- `deleteSave()`
  - 仅删除主存档，并清空主状态。

### 8.2 `stores/ui.ts`：UI 编排中台

#### 8.2.1 状态字段

- `currentPanel`
- `currentPanelComponent`
- `chatExpanded`
- `isCreatingCharacter`
- `activeLogChannel`
- `logs`

注意：

- `chatExpanded` 当前**仅定义未使用**；
- `logs` 是底部聊天/系统/战斗日志的统一数据源。

#### 8.2.2 核心动作

- `openPanel(panel)`
  - 播放点击音效；
  - 如当前仍在战斗，会先停止战斗并切回主场景；
  - 动态导入目标面板组件；
  - 如果打开的是 `city`，切主城 BGM。
- `closePanel()`
  - 本质是 `openPanel('city')`
- `hidePanel()`
  - 直接隐藏当前面板，不回到 `city`
- `pushLog(channel, text)`
  - 追加日志并截断为最多 80 条。

#### 8.2.3 面板注册表

`loadPanelComponent()` 目前已注册：

- `city`
- `character`
- `bag`
- `spirit`
- `skill`
- `bone`
- `codex`
- `quest`
- `rank`
- `arena`
- `sect`
- `worldMap`
- `shop`
- `smithy`
- `academy`
- `settings`

其中 `codex` 已注册但没有正常入口，详见“真实缺口”章节。

---

## 9. 数据层与类型层

### 9.1 `types/game.ts`

这个文件定义了项目全部核心类型：

- 阵营：`Faction`
- 武魂系别：`SpiritSeries`
- 魂环颜色：`RingColor`
- 品质：`Quality`
- 魂骨部位：`BoneSlot`
- 角色、角色属性、魂环、魂骨、地图、物品、快捷栏等接口

同时还包含：

- `FACTION_NAMES`
- `SERIES_NAMES`
- `RING_COLOR_MAP`
- `QUALITY_NAMES`
- `BONE_SLOT_NAMES`
- `RANK_TITLES`
- `getRankTitle(level)`

### 9.2 `data/gameData.ts`

这是项目的**原始静态数据源**。当前至少包括：

- `SPIRITS`
  - 约 105 个武魂，分强攻/敏攻/控制三系
- `SKILLS`
  - 与武魂系别和魂环槽位绑定的魂技表
- `BONES`
  - 50+ 魂骨定义
- `MAPS`
  - 12 张地图，带 `minLevel` 与 `faction` 限制
- `ITEMS`
  - 药品、材料、特殊道具、魂环类物品等
- `getExpToNext(level)`
  - 升级经验公式

### 9.3 `data/displayData.ts`

这个文件不是“主数据”，而是“显示修正层”，负责：

- 阵营文案映射
- 阵营说明
- 武魂系别文案
- 品质文案
- 魂骨部位文案
- 地图显示文案
- 常用物品显示文案与说明
- `displayName()` 统一显示名称
- `statLabel()` 统一属性中文名

也就是说：

- `gameData.ts` 偏“规则源”；
- `displayData.ts` 偏“界面文案源”。

---

## 10. 资源路径层与图标体系

### 10.1 `background-paths.ts`

直接导出三个背景资源：

- `cityWuhun`
- `battleXingdouOuter`
- `worldDouluoMap`

使用点：

- `CharacterCreate.vue`
- `CityPanel.vue`
- `WorldMapPanel.vue`
- `BootScene.ts`

### 10.2 `icon-paths.ts`

这是项目图标系统的入口文件，核心机制是：

- 用 `import.meta.glob('/src/assets/icons/**/*.png', { eager: true, import: 'default', query: '?url' })` 扫描静态图标；
- 用 `import.meta.glob('/src/assets/generated/items/*.png', ...)` 扫描生成物品图；
- 对不存在的资源生成 `SVG data URI` 占位图。

可用接口：

- `spiritIconPath(id)`
- `skillIconPath(id)`
- `boneIconPath(id)`
- `itemIconPath(id)`
- `systemIconPath(id)`
- `qualityFramePath(quality)`

特别说明：

- 魂环类物品如果没有真实 PNG，会生成专用魂环风格 SVG；
- 系统图标如果没有资源，也会用字母占位。

### 10.3 `generated-paths.ts`

负责统一访问 `src/assets/generated` 和 `src/assets/ui_generated` 下的 AI 生成资源。

典型接口：

- `generatedButtonPath()`
- `generatedBattleUiPath()`
- `generatedSectPath()`
- `generatedPortraitPath()`
- `generatedFighterPath()`
- `generatedItemPath()`
- `generatedSkillPath()`
- `cityLuxuryButtonPath()`

同时导出若干聚合对象：

- `generatedButtons`
- `generatedBattleUiButtons`
- `generatedSectImages`
- `generatedNpcPortraits`
- `generatedFighters`
- `cityLuxuryButtons`

### 10.4 `GameIcon.vue`

项目里几乎所有图标都经过这个组件显示。

它负责：

- 统一尺寸控制；
- 图片加载失败回退；
- `fallbackText` 占位字；
- `empty` 空槽样式；
- 基于 `quality` 的发光滤镜。

注意：`noFrame` 属性当前声明了，但组件内部**没有实际使用这项属性控制分支**。

---

## 11. UI 壳层与 HUD

### 11.1 `App.vue`

负责三行两列主布局：

- 顶部：`TopBar`
- 左侧：`LeftMenu`
- 中部：`GameContainer`
- 底部：`BottomBar`
- 叠加层：主城面板或其他业务面板

关键判断：

- `currentPanel === 'city'` 时显示 `CityPanel` 作为主城覆盖层；
- 其他情况显示动态组件面板；
- 创建角色中 `isCreatingCharacter = true` 时，不显示正常业务面板流。

### 11.2 `TopBar.vue`

依赖：`gameStore`、`uiStore`、`MAPS`、`displayData`、`systemIconPath`、固定头像资源。

职责：

- 显示角色头像、名称、等级、称号；
- 显示 HP/MP/EXP 条；
- 显示当前地图名与金币；
- 点击头像打开 `character` 面板；
- 点击地图 Chip 打开 `worldMap`。

### 11.3 `LeftMenu.vue`

依赖：`uiStore`、`systemIconPath`、`GameIcon`。

菜单项当前有：

- 主城
- 角色
- 背包
- 武魂
- 魂技
- 魂骨
- 竞技
- 宗门
- 任务
- 排行
- 设置

注意：**没有 `Codex` 菜单项**。

### 11.4 `BottomBar.vue`

依赖：`gameStore`、`uiStore`、`ITEMS`、`SKILLS`、`itemLabels`、图标路径、`audioManager`。

职责：

- 显示日志标签页：`全部 / 系统 / 提示 / 世界 / 战斗`
- 自动滚动到底部；
- 显示 9 个魂技槽（来自角色魂环）；
- 显示 8 个快捷物品槽（来自 `gameStore.quickSlots`）；
- 支持：
  - 点击快捷栏直接使用物品；
  - 从背包拖药品到快捷栏；
  - 右键清空快捷栏；
  - 使用失败写提示日志。

---

## 12. 面板系统逐一梳理

下面按“入口、主要依赖、关键逻辑、持久化/副作用”来梳理所有主面板。

### 12.1 `CharacterCreate.vue`

- 入口：首次无存档时由 `GameContainer.vue` 渲染。
- 依赖：`SPIRITS`、`displayData`、`gameStore`、`backgroundPaths`、`systemIconPath`。
- 流程：
  1. 选阵营；
  2. 随机觉醒武魂；
  3. 输入名字；
  4. 调用 `gameStore.createCharacter()`；
  5. 向父组件发出 `created` 事件。
- 特殊点：提供“快速进入测试角色”按钮。

### 12.2 `CityPanel.vue`

- 入口：`currentPanel === 'city'`
- 依赖：`uiStore`、主城背景、豪华按钮图。
- 行为：点击热点可进入：
  - 宗门
  - 世界地图
  - 学院
  - 任务大厅
  - 铁匠铺
  - 斗魂场
  - 商店

### 12.3 `CharacterPanel.vue`

- 入口：头像点击 / 左侧“角色”
- 依赖：`gameStore`、`uiStore`、`displayName`、`spiritIconPath`
- 行为：展示角色基础信息、战斗属性、魂环年限、武魂天赋。

### 12.4 `BagPanel.vue`

- 入口：左侧“背包”
- 依赖：`gameStore`、`uiStore`、`ITEMS`、`displayData`、`spiritRingDrops`、`inventoryHints`、`soulBoneCrafting`
- 标签页：`全部 / 材料 / 药品 / 魂骨 / 魂环 / 特殊`
- 关键行为：
  - 点击物品查看详情；
  - 药品可使用；
  - `scroll_s1` / `scroll_m1` 可强化第一魂环；
  - `spirit_ring_*` 道具可点击“吸收魂环”；
  - `bone_fragment` / `ancient_scroll` 可直接合成魂骨；
  - 支持出售 1 个 / 全部出售；
  - 支持拖药品到快捷栏；
  - 支持整理排序。
- 副作用：大量操作会进入 `gameStore.saveGame()`。

### 12.5 `RingSkillSelectPanel.vue`

- 入口：`GameContainer.vue` 常驻挂载，只在 `gameStore.pendingRingChoice` 存在时出现。
- 依赖：`gameStore`、`RING_COLOR_MAP`、`skillIconPath`
- 行为：
  - 展示当前待吸收魂环的槽位、颜色、年份；
  - 列出可选魂技；
  - 点击某个魂技调用 `gameStore.confirmRingChoice(skillId)`。

### 12.6 `SpiritPanel.vue`

- 入口：左侧“武魂”
- 依赖：`gameStore`、`RING_COLOR_MAP`、`seriesLabels`、`SKILLS`、图标路径
- 行为：
  - 展示武魂基础属性；
  - 展示 9 个魂环槽解锁状态；
  - 展示已装配魂技列表。

### 12.7 `SkillPanel.vue`

- 入口：左侧“魂技”
- 依赖：`gameStore`、`RING_COLOR_MAP`、`SKILLS`、显示标签
- 行为：
  - 展示已装配魂技；
  - 列出当前武魂系别、且尚未装备的可用魂技。
- 注意：这里是展示与查询，不负责装备切换。

### 12.8 `BonePanel.vue`

- 入口：左侧“魂骨”
- 依赖：`gameStore`、`BONES`、`displayData`、图标路径
- 标签页：`装备 / 图鉴 / 合成`
- 行为：
  - 装备页显示七部位魂骨位；
  - 图鉴页遍历全部魂骨定义；
  - 合成页当前是占位说明。
- 注意：真正的魂骨合成入口当前主要在 `BagPanel.vue`，不是这里。

### 12.9 `CodexPanel.vue`

- 入口：**已注册，但当前没有正常 UI 入口**
- 依赖：`SPIRITS`、`SKILLS`、图标路径
- 标签页：`武魂 / 魂技 / 魂兽`
- 行为：
  - 武魂图鉴：列武魂与属性；
  - 魂技图鉴：列魂技与环位；
  - 魂兽图鉴：当前使用本地硬编码列表，不直接复用 `REGION_MONSTERS`。
- 结论：这是一个可渲染面板，但当前属于“隐藏功能”。

### 12.10 `QuestPanel.vue`

- 入口：左侧“任务”或相关跳转
- 依赖：`gameStore`、`uiStore`、`ITEMS`、`itemLabels`、`generatedButtons`、`dailyQuestClaimKey()`
- 标签页：`主线 / 日常 / 宗门`
- 行为：
  - 用角色等级、经验、背包内容、魂环总年限、战力等动态拼出任务进度；
  - “前往”按钮直接 `uiStore.openPanel(targetPanel)`；
  - “领取”时给经验、金币、物品并持久化领取状态。
- 持久化：
  - 主线：`quest-claimed-main`
  - 向后兼容旧键：`quest-claimed`
  - 日常：`quest-claimed-daily-YYYY-MM-DD`

### 12.11 `RankPanel.vue`

- 入口：左侧“排行”
- 依赖：`gameStore`、`getRankTitle`、`FACTION_NAMES`
- 标签页：`等级 / 战力 / 竞技 / 财富`
- 行为：
  - AI 榜单 + 玩家当前角色混排；
  - 按不同字段排序。
- 注意：竞技胜场读取的是旧键 `arena-battles-today`，但 `ArenaPanel` 现在主写入的是日期化新键，因此这里存在**统计口径偏旧**的问题。

### 12.12 `ArenaPanel.vue`

- 入口：左侧“竞技” / 主城斗魂场热点
- 依赖：`gameStore`、`uiStore`、`SPIRITS`、图标路径、`generated-paths`、`battleSceneFlow`、`panelProgressionRules`
- 行为：
  - 基于角色等级与战力生成 AI 对手；
  - 调 `startBattleScene()` 进入真实战斗；
  - 胜负结算回到面板并更新积分、次数、奖励；
  - 支持积分商店兑换物品。
- 持久化：
  - `arena-points`
  - `arena-battles-today-YYYY-MM-DD`
  - 兼容读取旧键 `arena-battles-today`

### 12.13 `WorldMapPanel.vue`

- 入口：顶部地图按钮 / 主城世界地图热点 / 多处“前往狩猎”跳转
- 依赖：`gameStore`、`uiStore`、`MAPS`、`displayData`、`REGION_MONSTERS`、`monsterYearText()`、`battleSceneFlow`
- 行为：
  - 左侧世界地图点位选择区域；
  - 右侧列出该区域怪物；
  - 判断等级与阵营限制；
  - 点击“进入战斗”会：
    - 更新 `gameStore.currentMapId`
    - `saveGame()`
    - `uiStore.hidePanel()`
    - `startBattleScene()`
- 回战斗后：通过 `onVictory/onDefeat` 回调重新 `openPanel('worldMap')`。

### 12.14 `ShopPanel.vue`

- 入口：主城商店热点
- 依赖：`gameStore`、`ITEMS`、`generatedButtons`、`generatedNpcPortraits`
- 行为：
  - 列出药品与附魂石等商店货品；
  - 价格为 `Math.max(sellPrice * 5, 50)`；
  - 购买时扣金币并加物品。

### 12.15 `SmithyPanel.vue`

- 入口：主城铁匠铺热点
- 依赖：`gameStore`、图标路径、`generated-paths`
- 标签页：`暗器打造 / 魂骨合成 / 装备强化`
- 当前实现：
  - `forge` 页签可真实消耗材料 + 金币，生成道具；
  - `bone` / `enhance` 目前仍是占位卡片。

### 12.16 `AcademyPanel.vue`

- 入口：主城学院热点
- 依赖：`uiStore`、系统图标
- 当前实现：
  - 本地页面内状态的课程训练、每日考试、出题提示；
  - 没有接入 `gameStore` 数值成长。
- 结论：偏展示型/原型页。

### 12.17 `SectPanel.vue`

- 入口：左侧“宗门” / 主城宗门热点
- 依赖：`gameStore`、`uiStore`、系统图标、宗门生成资源
- 标签页：`信息 / 成员 / 技能 / 宗门战`
- 当前实现：
  - `sect-state` 本地保存宗门等级、经验、贡献、公告；
  - 成员列表以预设 NPC + 玩家自己混合；
  - 宗门技能可用贡献升级；
  - 宗门战为静态报名与战绩展示。

### 12.18 `SettingsPanel.vue`

- 入口：左侧“设置”
- 依赖：`uiStore`、`gameStore`
- 行为：
  - 音频、画面、游戏项设置；
  - 设置项本地持久化；
  - “复制存档”直接把 `chaoyue-dalu-save` 写到剪贴板；
  - “删除存档”会确认后调用 `gameStore.deleteSave()` 并刷新页面。
- 持久化：`game-settings`
- 注意：面板展示了热键文案，但当前全局热键切面板并未实现。

---

## 13. 关键业务链路总表

### 13.1 首次进入 -> 创建角色 -> 进入主界面

1. 打开页面；
2. `main.ts` 创建 Vue 应用；
3. `GameContainer.vue` 执行 `gameStore.loadGame()`；
4. 若无主存档：显示 `CharacterCreate.vue`；
5. 创建成功后：
   - `gameStore.createCharacter()` 初始化角色与背包；
   - `uiStore.isCreatingCharacter = false`；
   - `uiStore.openPanel('city')`；
   - 启动 Phaser；
6. 主界面出现顶栏、左栏、底栏、主城背景与主城热点。

### 13.2 已有存档 -> 恢复进入

1. `GameContainer.vue` 调 `gameStore.loadGame()`；
2. 读 `chaoyue-dalu-save`；
3. 标准化背包年份、快捷栏、魂环颜色、魂环槽位；
4. `recalculateStats()`；
5. 关闭创建角色流程；
6. 直接打开 `city` 面板并初始化 Phaser。

### 13.3 世界地图狩猎链路

1. `TopBar` 或 `CityPanel` 打开 `WorldMapPanel`；
2. 选择区域；
3. 选择怪物；
4. 点击“进入战斗”；
5. `gameStore.currentMapId = selectedRegion`；
6. `gameStore.saveGame()`；
7. `uiStore.hidePanel()`；
8. `startBattleScene(game, { region, monsterId, onVictory, onDefeat })`；
9. 战斗胜利：
   - 经验 + 金币 + 普通掉落；
   - 如果有空魂环位，再掉 1 个带年份的魂环道具；
10. 点击“继续冒险”后，通过回调重新回到 `WorldMapPanel`。

### 13.4 竞技场挑战链路

1. 打开 `ArenaPanel`；
2. 生成 AI 对手列表；
3. 点击“挑战”；
4. `uiStore.hidePanel()`；
5. `startBattleScene(game, { arenaOpponent, onVictory, onDefeat })`；
6. 战斗结束后：
   - 增减竞技积分；
   - 记录今日挑战次数；
   - 胜利额外给经验与金币；
   - 回到 `ArenaPanel`。

### 13.5 背包 -> 快捷栏链路

1. 背包中只有 `potion` 类型物品可拖拽；
2. `BagPanel` 在拖拽开始时写入 `application/x-dldl-item`；
3. `BottomBar` 物品槽响应 `drop`；
4. `gameStore.assignQuickSlot(slotIndex, itemId)`；
5. 点击底部快捷栏即可使用药品。

### 13.6 背包 -> 吸收魂环 -> 选魂技链路

1. 狩猎胜利后背包拿到 `spirit_ring_*`，且附带 `year`；
2. 在 `BagPanel` 选中魂环道具，点击“吸收魂环”；
3. `gameStore.openRingChoiceFromItem(itemId, year)`：
   - 找到下一个可吸收槽位；
   - 按主武魂系别筛出该槽可选魂技；
   - 生成 `pendingRingChoice`；
4. `RingSkillSelectPanel` 自动弹出；
5. 选择技能后 `confirmRingChoice(skillId)`：
   - 添加魂环；
   - 移除道具；
   - 重算属性并存档。

### 13.7 背包 -> 魂骨合成链路

1. 选中 `bone_fragment` 或 `ancient_scroll`；
2. 点击“合成魂骨”或“合成高阶魂骨”；
3. `gameStore.craftSoulBone(recipeId)`：
   - 检查材料；
   - 找空魂骨位；
   - 从目标品质候选池里按种子选一个魂骨；
   - 直接装备到角色身上；
   - 重算属性并存档。

### 13.8 任务领取链路

1. 打开 `QuestPanel`；
2. 根据角色与背包实时计算各任务进度；
3. 达成条件后 `canClaim = true`；
4. 点击“领取”后：
   - `addExp()`
   - `addGold()`
   - `addItem()`
   - 标记领取状态并写入本地。

### 13.9 设置 -> 删除存档链路

1. 打开 `SettingsPanel`；
2. 点击“删除存档”；
3. 浏览器 `confirm()`；
4. `gameStore.deleteSave()` 删除主存档并清空内存状态；
5. `uiStore.closePanel()`；
6. `location.reload()` 强制刷新。

---

## 14. `localStorage` 键位清单

| 键名 | 写入位置 | 含义 |
| --- | --- | --- |
| `chaoyue-dalu-save` | `stores/game.ts` | 主存档：角色、背包、快捷栏、当前地图 |
| `game-settings` | `SettingsPanel.vue` | 设置面板配置 |
| `sect-state` | `SectPanel.vue` | 宗门等级/经验/贡献/公告 |
| `arena-points` | `ArenaPanel.vue` | 竞技积分 |
| `arena-battles-today-YYYY-MM-DD` | `ArenaPanel.vue` | 每日竞技挑战次数（新键） |
| `arena-battles-today` | 旧兼容读取 | 旧版每日竞技次数键 |
| `quest-claimed-main` | `QuestPanel.vue` | 主线任务领取状态 |
| `quest-claimed` | 旧兼容读取 | 旧版任务领取状态键 |
| `quest-claimed-daily-YYYY-MM-DD` | `QuestPanel.vue` | 每日任务领取状态 |

重要结论：当前项目**没有统一的持久化中台**，而是“主存档集中、模块状态分散”。后续如果要做云存档/导入导出/重置某单模块，会涉及多处键位迁移。

---

## 15. 资源与脚本系统

### 15.1 `scripts/` 下的主要脚本

- `export-asset-manifest.mjs`
  - 从 `gameData.ts` 提取武魂/魂技/魂骨/物品/系统图标信息；
  - 生成 `asset-manifest.json` 和 `asset-manifest.md`。
- `export-image-prompts.mjs`
  - 基于资产清单生成图像提示词；
  - 输出到 `src/assets/art-direction/prompts/`。
- `export-atlas-groups.mjs`
  - 基于资产清单生成图集分组信息；
  - 输出 `icon-atlas-groups.json` 与 `icon-atlas-groups.md`。
- `verify-assets.mjs`
  - 用资产清单检查图标是否都在对应路径存在。

### 15.2 `src/assets/art-direction/` 的职责

这个目录不是普通静态资源目录，更像“**资源索引与资产中台**”。

它同时承载：

- 背景路径文件；
- 图标路径文件；
- 生成资源路径文件；
- 资产清单；
- 资源状态文档；
- 图集分组文档；
- 提示词样本与 JSONL。

### 15.3 `dist/` 状态

当前仓库中保留了 `dist/` 构建产物，说明项目构建曾成功执行，且输出包含大量静态资源与按面板拆分的产物文件。

---

## 16. 当前真实缺口与容易误判的地方

这一节非常关键，用来避免“看文档以为有，实际代码没接上”。

### 16.1 `CodexPanel.vue` 已注册，但当前没有正常入口

事实：

- `uiStore.loadPanelComponent()` 已注册 `codex`；
- `CodexPanel.vue` 可正常渲染；
- 但 `LeftMenu.vue` 没有图鉴菜单，其他主流程里也没有 `openPanel('codex')`。

结论：这是**可用但未暴露**的隐藏面板。

### 16.2 设置面板中的快捷键说明并不等于快捷键已实现

事实：

- `SettingsPanel.vue` 明面上展示了 `C/B/W/K/Q`；
- 项目全局代码里没有统一监听这些键来切换面板；
- 当前明确存在的键盘监听只有：
  - `main.ts` 用于音频解锁的 `keydown`
  - `BattleScene.ts` 中用于释放 1-9 魂技的数字键。

结论：**快捷键提示文案比当前实现更超前。**

### 16.3 `chatExpanded` 当前未使用

`uiStore` 中定义了 `chatExpanded`，但当前没有实际消费方，属于预留状态。

### 16.4 `GameScene.setAutoFight()` / `isAutoFighting()` 当前未被外部调用

主城场景预留了挂机/自动战斗状态接口，但当前没有模块真正使用它们。

### 16.5 `turnBattleRules.ts` 当前不是主战斗规则文件

当前真实战斗在 `BattleScene.ts + realtimeBattleRules.ts` 上运行，`turnBattleRules.ts` 更像旧的回合制残留辅助文件。

### 16.6 `RankPanel.vue` 的竞技胜场读取口径偏旧

它读取 `arena-battles-today`，但 `ArenaPanel.vue` 当前主要写入的是日期化键 `arena-battles-today-YYYY-MM-DD`。这会导致排行榜中的“竞技”统计可能不准确。

### 16.7 多个模块直接在组件里操作 `localStorage`

这意味着：

- 配置、宗门、任务、竞技并未统一纳入 `gameStore`；
- 后续想做“统一导出完整存档”时，需要同时汇总多个键。

### 16.8 没有自动化测试框架

当前仓库中未发现：

- `vitest.config.*`
- `jest.config.*`
- `playwright.config.*`

也没有对应测试目录，因此当前项目主要依赖：

- `vue-tsc`
- `vite build`
- 手工点点点验收

### 16.9 `public/` 目录很轻，绝大多数资源都走源码导入

当前资源组织更偏向模块导入与 Vite 打包，而不是传统 public 静态托管。

---

## 17. 推荐阅读顺序（给后续维护者）

如果要最快理解这个项目，建议按下面顺序看：

1. `package.json`
2. `main.ts`
3. `App.vue`
4. `GameContainer.vue`
5. `stores/ui.ts`
6. `stores/game.ts`
7. `types/game.ts`
8. `data/gameData.ts`
9. `data/displayData.ts`
10. `game/config.ts`
11. `BootScene.ts`
12. `GameScene.ts`
13. `BattleScene.ts`
14. `WorldMapPanel.vue`
15. `ArenaPanel.vue`
16. `BagPanel.vue`
17. `RingSkillSelectPanel.vue`
18. `SpiritPanel.vue` / `SkillPanel.vue` / `BonePanel.vue`
19. `icon-paths.ts` / `generated-paths.ts`
20. `scripts/` 下的资源脚本

---

## 18. 最终总结

当前项目已经具备一个很明确的原型结构：

- **Vue 壳层**负责主界面与全部业务面板；
- **Pinia 中台**负责主角色状态、背包、魂环、魂骨与 UI 面板切换；
- **Phaser 战斗层**负责主城展示与实时战斗；
- **资源索引层**通过 `import.meta.glob` 和生成路径文件统一管理图标、按钮、NPC、宗门图与背景；
- **本地持久化层**由一个主存档键 + 多个模块分散键组成。

如果未来要继续开发，最重要的三个扩展方向会是：

1. 把分散的 `localStorage` 状态收口到统一存档模型；
2. 把“存在但未接入口”的功能与“文案已写但逻辑未接”的功能对齐；
3. 给战斗、任务、竞技、背包等主链路补自动化验证。
